from flask import Flask, jsonify, send_from_directory
from flask_cors import CORS

app = Flask(__name__, static_folder='frontend/build', static_url_path='')
CORS(app)

@app.route('/')
def index():
    return send_from_directory(app.static_folder, 'index.html')

@app.route('/api/projects')
def get_projects():
    projects = [
        {
            "title": "Enhancing Park Safety Through Data-Driven Analysis in NYC Parks",
            "description": "Utilized Python and Tableau to analyze NYC park crime data...",
            "date": "January 2024 - March 2024"
        },
        {
            "title": "NCAA Tournament Prediction — ITC & GWU AI/ML Bracket Challenge",
            "description": "Conducted exploratory analysis on 21 years of NCAA data...",
            "date": "January 2024 - March 2024"
        }
    ]
    return jsonify(projects)

@app.route('/<path:path>')
def serve_static_file(path):
    return send_from_directory(app.static_folder, path)

if __name__ == '__main__':
    app.run(debug=True)
